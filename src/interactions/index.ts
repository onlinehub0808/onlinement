// Exporting the pageView function from the pageView module
export { pageView } from "./pageView";

// Exporting the event function from the event module
export { event } from "./event";

// Exporting the consent function from the consent module
export { consent } from "./consent";